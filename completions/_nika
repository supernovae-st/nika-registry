#compdef nika

autoload -U is-at-least

_nika() {
    typeset -A opt_args
    typeset -a _arguments_options
    local ret=1

    if is-at-least 5.2; then
        _arguments_options=(-s -S -C)
    else
        _arguments_options=(-s -C)
    fi

    local context curcontext="$curcontext" state line
    _arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'-V[Print version]' \
'--version[Print version]' \
":: :_nika_commands" \
"*::: :->nika" \
&& ret=0
    case $state in
    (nika)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(welcome)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--json[Emit the versioned machine projection (\`welcome_version\: 1\`)]' \
'--deep[The whole workspace truth (every workflow audited · recent runs · machine facts) — the deep half of the mirror (the old \`context\` verb, one roof)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(check)
_arguments "${_arguments_options[@]}" : \
'--profile=[Advisory displays the grade; operational fails at High/Unbounded]:PROFILE:((advisory\:"Grade displayed, never gating (the default)"
operational\:"Grade ≥ High fails the audit (exit 2)"))' \
'--model=[Price as if this \`<provider>/<model>\` replaced the envelope default]:MODEL:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--json[Machine projection (\`report_version\: 1\`)]' \
'--infer-permits[Print an inferred \`permits\:\` boundary]' \
'--fix[Apply typed rename repairs and re-audit]' \
'--native-strict[Fail when any \`native-first\` hint remains]' \
'--sdk-snapshot[Internal SDK adapter\: emit the report with its immutable snapshot]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'*::files -- Workflow file(s), `-`, or `registry\:owner/name\[@version\]`:_default' \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
'(--json)--output=[Print the typed \`outputs\:\` as ONE JSON object on stdout (progress → stderr) · the export contract · powers \`exec\: nika run sub.yaml --output json\` + \`capture\: stdout\`]:FORMAT:_default' \
'--model=[Override the workflow'\''s envelope \`model\:\` only (\`<provider>/<name>\`). Per-task \`model\:\` pins stay live and metered — \`--model\` does not descend into them (B22). Resolved through the SAME path as an envelope model — a bad id fails loud when an infer/agent task resolves it. \`--model mock/echo\` rehearses the envelope seat offline (zero key · zero network)]:PROVIDER/NAME:_default' \
'--access=[Pin the ACCESS path (\`model\:\` picks the intelligence; access picks the path) — either a class (\`local\` · \`mock\` · \`harness\` · \`oauth\` · \`api\`) or a harness seat (\`claude-code\` · \`codex\` · \`gemini-cli\` · \`kimi-code\` · \`qwen-code\`). Retired ACP wrapper ids (\`claude-agent-acp\` · \`codex-acp\`) refuse with NIKA-1802. A pin is a pin\: unsatisfied refuses before the prologue with a witness, never substitutes another path or model (D-2026-08-04-N1). Example\: \`--model openai/gpt-5.2 --access codex\` requests that model through the user'\''s Codex subscription seat]:PATH:_default' \
'*--var=[Set a workflow \`inputs\:\` value (repeatable). Overrides a declared \`default\:\` and satisfies a \`required\: true\` input. JSON when it parses (numbers · booleans · arrays), else a string. Unknown keys refused]:KEY=VALUE:_default' \
'(--dry-run)--resume=[Resume from a prior run'\''s NDJSON trace (\`nika run … --json > trace.ndjson\`)\: every task whose identity matches a journaled success is skipped with a visible \`task_cache_hit\` — an edited task or a changed input always re-runs (ADR-099). A trace without resume keys runs everything live (a notice, never an error). The trace'\''s tamper-evidence chain is VERIFIED before any record is trusted\: a broken chain refuses (exit 2), naming \`nika trace verify\` and the \`--resume-unverified\` opt-out. The trace'\''s recorded engine version is JUDGED (F-P21)\: a resume under a different engine refuses, naming both versions]:TRACE:_files' \
'--resume-compat=[Declare a cross-version resume compatible (F-P21 · NEP-0014 law 4)\: attests the trace recorded under engine \`<VERSION>\` may resume under this one — the token must name the trace'\''s recorded version exactly (\`unrecorded\` for a pre-versioning journal). The declared compat is journaled on the run'\''s boot manifest]:VERSION:_default' \
'--from=[Force this task AND its transitive downstream to re-run even on an identity match (the lever for changes the hashes cannot see — rotated secret · external state · an infer output to re-roll)]:TASK_ID:_default' \
'*--answer=[Answer a \`nika\:prompt\` gate (repeatable · ADR-099 rider)\: binds as the named task'\''s answer — \`--answer ok=true\` for confirm, a string for input, one of the choices for choice. The value parses as JSON when it parses, else rides as a string. Without \`--resume\` the answer is PRE-SEEDED on the fresh run\: it waits in the gate map and is consumed when the task asks (the CI one-pass gate)]:TASK=VALUE:_default' \
'(--resume)--task=[Run ONE task and its transitive upstream only (the regenerate-one- block move)\: the full workflow still audits (spans · findings stay whole-file faithful), then execution scopes to the ancestor sub-DAG and the plan/cost re-derive for exactly what will run. Workflow \`outputs\:\` are skipped (they may read unscoped tasks)]:TASK_ID:_default' \
'--max-cost-usd=[Operator run budget over METERED spend (USD). Refuses to start (exit 2) when the static cost floor already exceeds it; during the run the crossing call completes and counts, nothing new starts, unstarted tasks cancel and the run fails NIKA-1704 (exit 1) with spent-vs-budget — workflow \`outputs\:\` are not resolved on a budget stop (per-task values live in the trace). Spending EXACTLY the budget does not trip it. Costs use LIST RATES from the vendored public catalog — private/proxy/negotiated pricing is not reflected; local · mock · unpriced work is never blocked (the budget bounds what the catalog can meter)]:USD:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--json[Stream NDJSON events instead of the live render (CI · agents)]' \
'(--json --output)--no-progress[Plain render\: one final storyboard frame, no animation (the CI-stable surface · also the default when stdout is piped). A human surface — meaningless with the \`--json\`/\`--output\` machine modes, so refused there (the machine surface owns its rendering)]' \
'(--no-progress --json --output)--quiet[Quiet\: print only the final verdict card (errors always). A human surface · refused with \`--no-progress\` and the machine modes]' \
'(--output)--dry-run[Plan only — show the static plan and execute ZERO effects (spec §10). With \`--json\`\: ONE versioned plan object (\`plan_version\: 1\` — waves · cost ceiling · permits · requirements) instead of the human preview. \`--output\` stays refused (an outputs export of a run that never executed would be a lie)]' \
'--resume-unverified[Trust a \`--resume\` trace whose tamper-evidence chain FAILS the walk (ADR-099 trust amendment)\: the named opt-out, for a trace the operator edited or truncated by hand. The finding is journaled on the boot manifest (\`resume_unverified\: declared\`) — never a silent default; a verified trace journals no claim]' \
'--no-trace-file[Skip the run journal (\`.nika/traces/<ts>-<id>.ndjson\` · spec §3.3). Every run writes one by default so \`nika trace show|replay\`, \`--resume\` and the editor'\''s runs view have a file to read. \`NIKA_NO_TRACE_FILE\` (any non-empty value) opts out globally]' \
'--no-outputs[Hide the per-task output summaries (\`→ {…} · 312B\`) on the live storyboard. Interactive TTY only — pipes · CI · the machine modes never carry them anyway]' \
'--no-gc[Skip the opportunistic trace collection for this invocation (ADR-100\: \`.nika/traces/\` is bounded by default — retention rides every run start; a collection that removes anything says so on stderr)]' \
'--require-signature[Refuse to run an unsigned or invalidly-signed workflow (exit 2 · checked BEFORE any task executes). OPT-IN — default is unsigned-tolerant]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::file -- Workflow file (`*.nika.yaml`) · or a `registry\:owner/name\[@version\]` verified pull (cached + offline; `permits\:` never govern the fetch). OMITTED with exactly one workflow in this workspace → that one runs (announced); zero or several → the honest routing:_default' \
&& ret=0
;;
(test)
_arguments "${_arguments_options[@]}" : \
'*--answer=[Answer a blocking \`nika\:prompt\` without weakening it with a headless default (repeatable \`TASK=VALUE\`)]:TASK=VALUE:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--update[(Re)write the golden from this run instead of comparing]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::file -- Workflow file (`*.nika.yaml`):_default' \
&& ret=0
;;
(inspect)
_arguments "${_arguments_options[@]}" : \
'--format=[Project the graph instead of the human anatomy (json canonical · mermaid/dot derived — the docs/site surfaces)]:FORMAT:((json\:"Canonical JSON projection (\`graph_format\: 3\`)"
mermaid\:"Mermaid flowchart"
dot\:"Graphviz dot"
ascii\:"Terminal drawing (waves as columns · real wires · honest fallback)"))' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':file -- Workflow file (`*.nika.yaml`) · `-` reads stdin:_default' \
&& ret=0
;;
(explain)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--json[File form only\: emit the versioned machine projection (\`explain_version\: 1\` · the check report'\''s own vocabulary)]' \
'--forecast[File form only\: include the learned-truth forecast — duration/ cost/risk priors from YOUR local traces (stats over \`.nika/traces/\` · never a model call · never the network)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':code -- An error code (`NIKA-440` · bare `440` · `DAG-003`), a hint identity (`jq-as-map` · `native-first/006`), or a workflow file path (`*.nika.yaml` · `-` reads stdin):_default' \
&& ret=0
;;
(key)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_nika__subcmd__key_commands" \
"*::: :->key" \
&& ret=0

    case $state in
    (key)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-key-command-$line[1]:"
        case $line[1] in
            (init)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--force[Overwrite an existing key (a rotation without the retired-pub ledger entry)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(trust)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(rotate)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__key__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-key-help-command-$line[1]:"
        case $line[1] in
            (init)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(trust)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(rotate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(arm)
_arguments "${_arguments_options[@]}" : \
'--emit=[Emit the OS unit that fires the beats (\`launchd\` · \`systemd\`) instead of reading the registry — the W3 wave]:OS:((launchd\:"macOS launchd user agent (\`~/Library/LaunchAgents/nika.arm.<radical>.plist\`)"
systemd\:"A systemd user timer + service pair"))' \
'--out=[With \`--emit --write\`\: the directory the unit writes to]:DIR:_files' \
'--mode=[With \`--emit\`\: the scope the unit installs at]:SCOPE:((user\:"The operator'\''s own agent (the default posture)"
system\:"A system-wide daemon"))' \
'--env-file=[With \`--emit\`\: the env file the unit loads (provider keys live there, never in the unit)]:FILE:_files' \
'--nika-bin=[With \`--emit\`\: the nika binary the unit invokes]:BIN:_files' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--write[With \`--emit\`\: write the unit file instead of printing it]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_nika__subcmd__arm_commands" \
"*::: :->arm" \
&& ret=0

    case $state in
    (arm)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-arm-command-$line[1]:"
        case $line[1] in
            (fire)
_arguments "${_arguments_options[@]}" : \
'--now=[Inject the decision instant (RFC 3339) instead of reading the wall clock — the clock is the verb'\''s edge (D5), so a replay and a test are deterministic]:RFC3339:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':label -- The beat label — the workflow file radical (`workflows/doctor.nika.yaml` → `doctor`; a radical collision in file order takes `-2`, `-3`):_default' \
&& ret=0
;;
(migrate)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(disarm)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--write[Also tear the OS unit down — the W3 wave]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':label -- The beat label (the workflow file radical):_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__arm__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-arm-help-command-$line[1]:"
        case $line[1] in
            (fire)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(migrate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(disarm)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(serve)
_arguments "${_arguments_options[@]}" : \
'--now=[Inject the clock (RFC 3339 · D5) — the harness]:RFC3339:_default' \
'--until=[Stop the loop at this instant (RFC 3339) — the harness]:RFC3339:_default' \
'--bind=[Bind an authenticated HTTP listener. Requires \`--workflows\` and \`--token-file\`]:ADDR:_default' \
'--workflows=[Held registry root of \`.nika.yaml\` workflows. Requires \`--bind\`]:DIR:_files' \
'--token-file=[Owner-only Bearer file (32–512 visible ASCII bytes, mode 0600). Never argv. Mint\: umask 077 && openssl rand -hex 24 > .nika/serve.token && chmod 600 .nika/serve.token]:FILE:_files' \
'--state-root=[Durable job-state root. Defaults to \`<cwd>/.nika/serve\`]:DIR:_files' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--once[Fire what is due once, then exit — the rehearsal]' \
'--dry[Say what WOULD fire, run nothing]' \
'--allow-remote[Acknowledge a non-loopback \`--bind\`. Authentication is unchanged. TLS is a reverse proxy — this process does not terminate it]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(sign)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--check[Verify the \`<file>.minisig\` sidecar instead of minting it (exits\: 0 valid · 2 FILE invalid/forged · 3 ENV missing/none)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':file -- Workflow file (`*.nika.yaml`) to sign — or to verify with `--check`:_default' \
&& ret=0
;;
(doctor)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ping[TCP-probe the local provider ports (loopback/configured only · 300ms cap · nothing is sent on the socket). Offline without it]' \
'--json[Emit the machine projection (summary + findings\[\] — agents/CI branch on \`summary.fail\` instead of parsing glyphs)]' \
'--verbose[Unfold every advisory note (an unwired agent · an unconfigured provider · the config-less default) — the calm default folds them into ONE line (B-8b · a healthy machine reads calm)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(init)
_arguments "${_arguments_options[@]}" : \
'--recipe=[Scaffold a workflow set — the wizard'\''s recipe step, scriptable (\`agentic\` = the 4-pattern curriculum)]:RECIPE:(agentic starter ship content minimal)' \
'(--recipe)--example=[Found the project from ONE embedded example (verbatim — any slug from bare \`nika try\`). One founding source\: conflicts with \`--recipe\`]:SLUG:_default' \
'--theme=[Stamp the VS Code DAG canvas skin (\`nika.dag.theme\`) into the created \`.vscode/settings.json\`]:THEME:((nika\:"The brand skin — engineered black · verb hues"
editor\:"Adaptive — follows the editor'\''s colors"
phosphor\:"Terminal green"
auto\:"Let the extension decide"))' \
'*--wire=[Wire agent clients to the MCP oracle after the scaffold (comma-separated · the same targets as \`nika wire\`)]:WIRE:((cursor\:""
vscode\:""
windsurf\:""
claude\:""
claude-desktop\:""
cline\:""
codex\:""
continue\:""
zed\:""
opencode\:""
hermes\:""
gemini\:""
qwen\:""
lmstudio\:""
junie\:""
grok\:""
antigravity\:""
kimi\:""
kiro\:""
copilot\:""
amp\:""
detected\:"Only the clients THIS machine shows (the probe'\''s presence truth) — the recommended door\: \`wire detected --dry-run\`, then \`wire detected\`"
all\:"Every supported client — the advanced door\: preview with \`--dry-run\`, confirm live on a terminal, or pass \`--yes\` in a pipe"))' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--force[Overwrite existing files]' \
'-y[Accept every default — never prompt (pipes and CI are implicitly \`--yes\`; prompts only ever appear on a terminal)]' \
'--yes[Accept every default — never prompt (pipes and CI are implicitly \`--yes\`; prompts only ever appear on a terminal)]' \
'--project-file[Lay a starter \`nika.yaml\` (the project file — ceiling + retention examples, commented so the starter governs nothing until you edit it). The ONLY scripted door; the wizard lane asks instead. An existing file is skipped, \`--force\` overrides]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::dir -- Target directory (default · the current directory):_default' \
&& ret=0
;;
(wire)
_arguments "${_arguments_options[@]}" : \
'--dir=[Workspace directory for repo-local clients such as VS Code]:DIR:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--dry-run[Print the per-client plan (created/updated/current/manual) — writes nothing]' \
'--yes[Consent to \`all\` without a prompt (scripts · CI — a terminal asks)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':target -- Client to wire (`detected` = only the clients this machine shows):((cursor\:""
vscode\:""
windsurf\:""
claude\:""
claude-desktop\:""
cline\:""
codex\:""
continue\:""
zed\:""
opencode\:""
hermes\:""
gemini\:""
qwen\:""
lmstudio\:""
junie\:""
grok\:""
antigravity\:""
kimi\:""
kiro\:""
copilot\:""
amp\:""
detected\:"Only the clients THIS machine shows (the probe'\''s presence truth) — the recommended door\: \`wire detected --dry-run\`, then \`wire detected\`"
all\:"Every supported client — the advanced door\: preview with \`--dry-run\`, confirm live on a terminal, or pass \`--yes\` in a pipe"))' \
&& ret=0
;;
(model)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_nika__subcmd__model_commands" \
"*::: :->model" \
&& ret=0

    case $state in
    (model)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-model-command-$line[1]:"
        case $line[1] in
            (serve)
_arguments "${_arguments_options[@]}" : \
'--model=[The model\: a \`.gguf\` path, or a pulled id — \`owner/repo\[\:QUANT\]\` or a file stem, resolved against the models dir (\`nika model list\`)]:PATH|ID:_default' \
'--tokenizer=[The tokenizer file (default\: \`tokenizer.json\` beside the model)]:PATH:_files' \
'--port=[Loopback port to listen on]:PORT:_default' \
'--model-id=[The model id responses report (default\: the model file'\''s name)]:ID:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(pull)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'-y[Skip the size confirmation (CI · scripts)]' \
'--yes[Skip the size confirmation (CI · scripts)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':model -- The Hub model\: `owner/repo\[\:QUANT\]` — default quant `Q4_K_M` when the repo tags one:_default' \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(rm)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':id -- The id to remove (`owner/repo\[\:QUANT\]`, or a file stem from `list`):_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__model__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-model-help-command-$line[1]:"
        case $line[1] in
            (serve)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pull)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(rm)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(spec)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--canon[Print the canon.yaml single source of truth]' \
'(--canon)--schema[Print the embedded JSON Schema for \`*.nika.yaml\` (the old \`schema\` verb, one roof)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(catalog)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--json[Emit the versioned machine projection (\`catalog_version\: 1\`)]' \
'--tools[The \`nika\:*\` builtin tool catalog instead (what \`invoke\` reaches without MCP — the old \`tools\` verb, one roof)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(try)
_arguments "${_arguments_options[@]}" : \
'--model=[Run on a REAL seat instead of the default mock rehearsal (\`<provider>/<name>\` — the example'\''s own \`model\:\` via \`self\`)]:PROVIDER/NAME:_default' \
'--access=[Pin the ACCESS path for the rehearsal (a class — \`local\` · \`api\` · \`harness\` · \`oauth\` · \`mock\` — or an access id \`nika doctor\` lists). Unsatisfied refuses with a witness, never substitutes]:PATH:_default' \
'*--var=[Set a workflow \`inputs\:\` value (repeatable)]:KEY=VALUE:_default' \
'--max-cost-usd=[Refuse to start if the static cost floor exceeds this (USD); metered spend aborts past it mid-run. Same guard, same parser as \`nika run\`]:USD:_default' \
'--resume=[Resume from a prior run'\''s NDJSON trace — same flag, same parser as \`nika run --resume\`. Relative paths pin to the operator cwd (the rehearsal then enters a temp room)]:TRACE:_files' \
'*--answer=[Answer a \`nika\:prompt\` gate (repeatable · same parser as \`nika run --answer\`). Without \`--resume\` the answer is PRE-SEEDED on the fresh rehearsal (the CI one-pass gate)]:TASK=VALUE:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--all[The whole shelf — the numbered path plus every job (bare \`nika try\` shows the pack first-run shelf)]' \
'--quiet[Verdict line only (suppress the storyboard)]' \
'--no-progress[One final storyboard frame (no live repaints) — pipes/CI get this automatically]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::slug -- Example slug (bare `nika try` shows the storefront):_default' \
&& ret=0
;;
(new)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--force[Overwrite an existing destination]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::intent -- Plain-words intent, an example slug, or a skeleton name (`'\''?'\''` lists the set). Omitted on a terminal → the guided three-question flow; omitted in a pipe → fail fast:_default' \
'::dest -- Destination path (`*.nika.yaml`) — defaults to `<slug>.nika.yaml` beside you:_default' \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':shell -- Target shell:(bash elvish fish powershell zsh)' \
&& ret=0
;;
(trace)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_nika__subcmd__trace_commands" \
"*::: :->trace" \
&& ret=0

    case $state in
    (trace)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-trace-command-$line[1]:"
        case $line[1] in
            (replay)
_arguments "${_arguments_options[@]}" : \
'--speed=[Replay time compression (6 = 6× faster than recorded)]:SPEED:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'()--demo[Render the built-in success storyboard]' \
'(--demo)--demo-fail[Render the built-in failure storyboard]' \
'--no-outputs[Hide the per-task output summaries (\`→ {…} · 312B\`) on the rendered storyboard. Interactive TTY only — a piped \`trace show\` never carries them anyway]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::trace -- Trace NDJSON path (one `nika-event` Event per line):_files' \
&& ret=0
;;
(evidence)
_arguments "${_arguments_options[@]}" : \
'-o+[Output directory (default\: \`<trace-stem>.evidence/\` · never clobbered)]:OUT:_files' \
'--out=[Output directory (default\: \`<trace-stem>.evidence/\` · never clobbered)]:OUT:_files' \
'--workflow=[The workflow file that ran — hash-checked; unlocks the boundary, the trifecta verdict and the receipt]:WORKFLOW:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--json[Print the pack manifest to stdout (no directory written)]' \
'--full[Carry the run'\''s CONTENT verbatim (model outputs · tool results · file reads). Default\: the redacted pack — it proves the run'\''s INTEGRITY, not its CONTENT; disclosure stays operator-side]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::trace -- Trace NDJSON path or a name from `trace ls` (default\: latest):_files' \
&& ret=0
;;
(receipt)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_nika__subcmd__trace__subcmd__receipt_commands" \
"*::: :->receipt" \
&& ret=0

    case $state in
    (receipt)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-trace-receipt-command-$line[1]:"
        case $line[1] in
            (explain)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':file -- The receipt JSON file (an evidence pack'\''s `receipt.json`):_files' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__trace__subcmd__receipt__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-trace-receipt-help-command-$line[1]:"
        case $line[1] in
            (explain)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(show)
_arguments "${_arguments_options[@]}" : \
'--speed=[Replay time compression (6 = 6× faster than recorded)]:SPEED:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'()--demo[Render the built-in success storyboard]' \
'(--demo)--demo-fail[Render the built-in failure storyboard]' \
'--no-outputs[Hide the per-task output summaries (\`→ {…} · 312B\`) on the rendered storyboard. Interactive TTY only — a piped \`trace show\` never carries them anyway]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::trace -- Trace NDJSON path (one `nika-event` Event per line):_files' \
&& ret=0
;;
(ls)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(rm)
_arguments "${_arguments_options[@]}" : \
'(--all)--older-than=[Remove every trace older than this (\`45s\` · \`30m\` · \`12h\` · \`7d\`)]:DURATION:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--all[Remove every trace in the store]' \
'--force[Remove even a paused trace (destroys its unanswered prompt)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::trace -- The trace to remove — a name from `trace ls` or a path:_default' \
&& ret=0
;;
(outputs)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::trace -- Trace NDJSON path (default\: the workspace'\''s latest trace):_files' \
&& ret=0
;;
(export)
_arguments "${_arguments_options[@]}" : \
'-o+[Output path (default\: \`<trace>.otlp.jsonl\` beside the journal)]:OUT:_files' \
'--out=[Output path (default\: \`<trace>.otlp.jsonl\` beside the journal)]:OUT:_files' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--include-content[Include recorded task outputs as span attributes (payloads stay LOCAL either way — this only widens the exported file)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':trace -- Trace NDJSON path (one `nika-event` Event per line):_files' \
&& ret=0
;;
(verify)
_arguments "${_arguments_options[@]}" : \
'--key=[A candidate run public key for the SEALED tier (default\: ~/.nika/keys/run-signing.pub, then the retired.pub ledger)]:KEY:_files' \
'--replay=[The REPLAYED tier\: a FRESH journal of the same workflow to compare against (verify never re-executes)]:REPLAY:_files' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--anchored[Require the anchor tier\: a MISSING sidecar is exit 3 (a forged one is exit 2 either way)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'*::traces -- Trace NDJSON path(s) — a shell glob (`.nika/traces/*.ndjson`) just works\: each file verifies under its own header, the worst exit survives (default\: the workspace'\''s latest trace):_files' \
&& ret=0
;;
(anchor)
_arguments "${_arguments_options[@]}" : \
'--rekor-url=[The Rekor v2 shard. A private rekor-tiles deployment works, but its checkpoint is not the pinned Sigstore key'\''s — the ANCHORED verify tier stays out of reach there]:REKOR_URL:_default' \
'--tsa-url=[The RFC 3161 timestamp authority. The token verifies against the pinned Sigstore TSA leaf — mirrors of that TSA work, other authorities fail closed]:TSA_URL:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::trace -- Trace NDJSON path (default\: the workspace'\''s latest trace):_files' \
&& ret=0
;;
(reproduce)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':recorded -- The RECORDED journal (the reference frame):_files' \
':fresh -- A FRESH journal of the same workflow (run it again first):_files' \
&& ret=0
;;
(peek)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--raw[Print the exact recorded value only (machine-friendly)]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':trace -- Trace NDJSON path (one `nika-event` Event per line):_files' \
':task -- The task id whose output to read:_default' \
&& ret=0
;;
(session)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':trace -- Trace NDJSON path (one `nika-event` Event per line):_files' \
':workflow -- The workflow file the run executed (`*.nika.yaml`):_default' \
&& ret=0
;;
(flow)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
'::trace -- Trace NDJSON path (default\: the workspace'\''s latest trace — `nika trace flow <workflow>` alone reads the last run):_files' \
'::workflow -- The workflow file the run executed (`*.nika.yaml`) — the trace records values, the definition records the bindings:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__trace__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-trace-help-command-$line[1]:"
        case $line[1] in
            (replay)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(evidence)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(receipt)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__trace__subcmd__help__subcmd__receipt_commands" \
"*::: :->receipt" \
&& ret=0

    case $state in
    (receipt)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-trace-help-receipt-command-$line[1]:"
        case $line[1] in
            (explain)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(show)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(ls)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(rm)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(outputs)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(export)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(verify)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(anchor)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(reproduce)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(peek)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(session)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(flow)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(guard)
_arguments "${_arguments_options[@]}" : \
'(--stdin)--command=[Judge ONE shell command line instead of a hook payload]:LINE:_default' \
'--cwd=[The directory the command runs in (with \`--command\`; the payload'\''s \`cwd\` wins on the stdin wire, the process cwd otherwise)]:DIR:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--stdin[Read the host hook JSON payload from stdin (the shim'\''s wire\: Cursor \`{command, cwd}\` · Claude Code \`PreToolUse\` \`{tool_input\:{command}, cwd}\` — sniffed by \`hook_event_name\`)]' \
'--human[The human reading (allow · deny · \`guard_unavailable\` + why) instead of the hook JSON protocol]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(dap)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(lsp)
_arguments "${_arguments_options[@]}" : \
'--clientProcessId=[Same convention family\: hosts pass their own PID so a server can watchdog its parent — and this one does. A host that closes the pipe ends the session by EOF; a host that CRASHES does not, and the server would outlive it forever (#1181). Hidden\: no human types it, the editor does]:CLIENT_PROCESS_ID:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--stdio[LSP-host convention flag\: vscode-languageclient, nvim and helix spawn \`<server> --stdio\` by habit. Stdio is this server'\''s ONLY transport, so the flag is a no-op — but refusing it killed every spawn from a client that passes it, with exit 2 before the first byte of JSON-RPC (the v0.106.0 extension post-mortem\: the language server had never once run in production because of this refusal). Hidden\: it teaches nothing a human needs to type]' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
&& ret=0
;;
(mcp)
_arguments "${_arguments_options[@]}" : \
'--transport=[The wire\: \`stdio\` (the editor/agent default) or \`http\` (Streamable HTTP · POST JSON-RPC · spec 2025-11-25)]:TRANSPORT:((stdio\:"Newline-delimited JSON-RPC over stdin/stdout"
http\:"Streamable HTTP (POST JSON-RPC · origin-gated · loopback default)"))' \
'--port=[HTTP port (with \`--transport http\`)]:PORT:_default' \
'--bind=[HTTP bind address. Loopback by default — widening this exposes the server to your network; put TLS + auth (a reverse proxy · \`NIKA_MCP_TOKEN\`) in front before you do]:BIND:_default' \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
":: :_nika__subcmd__mcp_commands" \
"*::: :->mcp" \
&& ret=0

    case $state in
    (mcp)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-mcp-command-$line[1]:"
        case $line[1] in
            (approve)
_arguments "${_arguments_options[@]}" : \
'--color=[When to colour the output (auto = TTY + \`TERM != dumb\` · honours \`CLICOLOR_FORCE\` · \`NO_COLOR\` · \`CLICOLOR=0\` in that order)]:COLOR:((always\:"Force colour on (pagers accepting escapes · captured demos)"
never\:"Force colour off (the \`--no-color\` flags fold here)"
auto\:"Resolve from the environment chain + TTY (the default)"))' \
'--hyperlink=[When to emit OSC-8 hyperlinks on printed paths (auto = TTY + \`TERM != dumb\` · never to pipes; always = force them, for pagers that pass escapes — tmux/screen may render them as plain text)]:HYPERLINK:((always\:"Force hyperlinks on (escape-passing pagers · captured demos)"
never\:"Force hyperlinks off"
auto\:"TTY + \`TERM != dumb\` — never to pipes (the default)"))' \
'--ascii[Force the ASCII glyph twins everywhere (CI logs · legacy terminals) — colour stays; \`--plain\` is the full sober umbrella]' \
'--plain[The sober umbrella — one flag for scripts, CI and transcripts\: colour off · ASCII glyphs · hyperlinks off · no animation (\`run\` renders its plain storyboard). The same result as \`--color never --hyperlink never\` plus every verb'\''s \`--ascii\`/\`--no-progress\`]' \
'-h[Print help (see more with '\''--help'\'')]' \
'--help[Print help (see more with '\''--help'\'')]' \
':server -- The server name from `.nika/mcp_servers.json`:_default' \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__mcp__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-mcp-help-command-$line[1]:"
        case $line[1] in
            (approve)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
;;
(help)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__help_commands" \
"*::: :->help" \
&& ret=0

    case $state in
    (help)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-help-command-$line[1]:"
        case $line[1] in
            (list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(welcome)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(check)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(run)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(test)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(inspect)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(explain)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(key)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__help__subcmd__key_commands" \
"*::: :->key" \
&& ret=0

    case $state in
    (key)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-help-key-command-$line[1]:"
        case $line[1] in
            (init)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(trust)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(rotate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(arm)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__help__subcmd__arm_commands" \
"*::: :->arm" \
&& ret=0

    case $state in
    (arm)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-help-arm-command-$line[1]:"
        case $line[1] in
            (fire)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(migrate)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(disarm)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(serve)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(sign)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(doctor)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(init)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(wire)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(model)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__help__subcmd__model_commands" \
"*::: :->model" \
&& ret=0

    case $state in
    (model)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-help-model-command-$line[1]:"
        case $line[1] in
            (serve)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(pull)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(list)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(rm)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(spec)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(catalog)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(try)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(new)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(completions)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(trace)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__help__subcmd__trace_commands" \
"*::: :->trace" \
&& ret=0

    case $state in
    (trace)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-help-trace-command-$line[1]:"
        case $line[1] in
            (replay)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(evidence)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(receipt)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__help__subcmd__trace__subcmd__receipt_commands" \
"*::: :->receipt" \
&& ret=0

    case $state in
    (receipt)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-help-trace-receipt-command-$line[1]:"
        case $line[1] in
            (explain)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(show)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(ls)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(rm)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(outputs)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(export)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(verify)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(anchor)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(reproduce)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(peek)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(session)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(flow)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(guard)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(dap)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(lsp)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
(mcp)
_arguments "${_arguments_options[@]}" : \
":: :_nika__subcmd__help__subcmd__mcp_commands" \
"*::: :->mcp" \
&& ret=0

    case $state in
    (mcp)
        words=($line[1] "${words[@]}")
        (( CURRENT += 1 ))
        curcontext="${curcontext%:*:*}:nika-help-mcp-command-$line[1]:"
        case $line[1] in
            (approve)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
(help)
_arguments "${_arguments_options[@]}" : \
&& ret=0
;;
        esac
    ;;
esac
;;
        esac
    ;;
esac
}

(( $+functions[_nika_commands] )) ||
_nika_commands() {
    local commands; commands=(
'list:List the workflows below this directory, one relative path per line' \
'welcome:The mirror\: what Nika is · what this machine already has (editors · local models · key presence · this workspace) · the next commands. Offline · presence-only · always exit 0 — a greeting, not a gate' \
'check:Audit a workflow BEFORE it runs\: plan · cost ceiling · secret flows · types · tools — every finding teaches its fix' \
'run:Run a workflow (the same audit runs first · live render)' \
'test:Golden test\: run under the MOCK provider (offline · deterministic) and compare the typed \`outputs\:\` against \`<file>.golden.json\`' \
'inspect:Static anatomy\: tasks · verbs · wave groups · cost · permits — and the ONE graph projector (\`--format json|mermaid|dot\` for the machine surfaces · human stays the default)' \
'explain:Teach one error code (cause · category · fix-form), a hint kind \`nika check\` printed in \`\[brackets\]\`, or narrate a workflow FILE\: what it does · the waves · cost before a token is spent · what it touches · how to run it' \
'key:The run-signing key lifecycle (mint · TOFU fingerprint · rotate — old pubs stay verifiable)' \
'arm:What this project has ARMED, and when each beat next fires. Read-only — it schedules nothing (the file proposes, the machine disposes). Exit \`0\` clean · \`2\` the registry refuses' \
'serve:Resident ARM firer by default (the SAME \`fire\`, wall clock in place of the OS). \`--bind\` + \`--workflows\` + \`--token-file\` opens authenticated loopback HTTP. Exit \`0\` clean · \`1\` otherwise' \
'sign:Sign a workflow file (S3 · author-binding)\: mint \`<file>.minisig\` · \`--check\` verifies' \
'doctor:Diagnose this machine (binary · config · provider keys · local models). Diagnose-only — prints the exact fix command, never mutates anything' \
'init:Found a repo (\`.vscode\` schema wiring · \`AGENTS.md\` · Cursor rule + MCP · \`.agents/skills\` authoring skill · optional workflow set). Bare on a terminal the founding wizard runs; flags are the scriptable twin. Existing files are skipped — \`--force\` overwrites' \
'wire:Wire Nika into editor/agent MCP clients (explicit, idempotent). The door\: \`detected --dry-run\` previews what this machine shows · \`detected\` wires it · \`<client>\` wires one · \`all\` is the advanced sweep (previewed, then confirmed or \`--yes\`)' \
'model:Local models — pull from the Hugging Face Hub, serve on this machine, list/rm the disk (ONE models dir · no external daemon)' \
'spec:The embedded spec identity (\`--canon\` prints the SSOT)' \
'catalog:The embedded provider/model catalog (models · capabilities · env vars)' \
'try:See a canonical workflow WORK — offline by default (the mock rehearsal · zero keys · zero flags), nothing written, nothing owned. Bare \`nika try\` lists what there is to see' \
'new:The ONE creation door\: describe the job in plain words (routes), or name a slug/skeleton (takes it, ingredients included) — the destination derives from the slug. Plain words route across jobs, lessons and skeletons. Bare \`nika new\` on a terminal is the guided flow; \`nika new '\''?'\''\` lists the skeleton set' \
'completions:Generate shell completions (bash · zsh · fish · elvish · powershell)' \
'trace:Read the flight recorder (replay or summarize a run)' \
'guard:The hook'\''s judge (hidden — the wired \`guard-run.sh\` shim calls it, agents never type it)\: read a host hook payload (\`--stdin\`) or one command line (\`--command\`), find every effective \`nika run\`, audit the EXACT file in-process, and answer the hook protocol. P0-7 + P0-15\: a red file or a priced model without \`--max-cost-usd\` is denied; an unjudgeable run is a VISIBLE \`guard_unavailable\`, never a silent allow. The run belongs to the human — guard JUDGES, it never executes' \
'dap:Debug Adapter Protocol server (stdio) — time-travel a recorded run under a debugger UI\: breakpoints on task lines · step forward AND back through settles · outputs in the variables pane. Replay re-renders, never re-executes' \
'lsp:Run the language server over stdio (drives the editor extension)' \
'mcp:Run the read-only MCP oracle (validate + learn — never execute). Default transport\: stdio; \`--transport http\` serves Streamable HTTP for managed hosts. \`approve\` re-pins tools from \`.nika/mcp_servers.json\`' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika commands' commands "$@"
}
(( $+functions[_nika__subcmd__arm_commands] )) ||
_nika__subcmd__arm_commands() {
    local commands; commands=(
'fire:Fire ONE beat now, if it is due — the one firer (D2)\: on-time window · miss policy · overlap lock · per-tick ceiling · the firing record. Prints exactly one stdout line, always (D8)' \
'migrate:Explicitly upcast every W2 sidecar into the hash-chained ledger, verify each chain, and rebuild its projections. Idempotent and never silent\: every beat receives a verdict' \
'disarm:Teach the disarm gesture (law N4 — removing the line does NOT disarm; \`actif\: false\` + \`raison\:\` + \`jusqu_au\:\` does)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika arm commands' commands "$@"
}
(( $+functions[_nika__subcmd__arm__subcmd__disarm_commands] )) ||
_nika__subcmd__arm__subcmd__disarm_commands() {
    local commands; commands=()
    _describe -t commands 'nika arm disarm commands' commands "$@"
}
(( $+functions[_nika__subcmd__arm__subcmd__fire_commands] )) ||
_nika__subcmd__arm__subcmd__fire_commands() {
    local commands; commands=()
    _describe -t commands 'nika arm fire commands' commands "$@"
}
(( $+functions[_nika__subcmd__arm__subcmd__help_commands] )) ||
_nika__subcmd__arm__subcmd__help_commands() {
    local commands; commands=(
'fire:Fire ONE beat now, if it is due — the one firer (D2)\: on-time window · miss policy · overlap lock · per-tick ceiling · the firing record. Prints exactly one stdout line, always (D8)' \
'migrate:Explicitly upcast every W2 sidecar into the hash-chained ledger, verify each chain, and rebuild its projections. Idempotent and never silent\: every beat receives a verdict' \
'disarm:Teach the disarm gesture (law N4 — removing the line does NOT disarm; \`actif\: false\` + \`raison\:\` + \`jusqu_au\:\` does)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika arm help commands' commands "$@"
}
(( $+functions[_nika__subcmd__arm__subcmd__help__subcmd__disarm_commands] )) ||
_nika__subcmd__arm__subcmd__help__subcmd__disarm_commands() {
    local commands; commands=()
    _describe -t commands 'nika arm help disarm commands' commands "$@"
}
(( $+functions[_nika__subcmd__arm__subcmd__help__subcmd__fire_commands] )) ||
_nika__subcmd__arm__subcmd__help__subcmd__fire_commands() {
    local commands; commands=()
    _describe -t commands 'nika arm help fire commands' commands "$@"
}
(( $+functions[_nika__subcmd__arm__subcmd__help__subcmd__help_commands] )) ||
_nika__subcmd__arm__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'nika arm help help commands' commands "$@"
}
(( $+functions[_nika__subcmd__arm__subcmd__help__subcmd__migrate_commands] )) ||
_nika__subcmd__arm__subcmd__help__subcmd__migrate_commands() {
    local commands; commands=()
    _describe -t commands 'nika arm help migrate commands' commands "$@"
}
(( $+functions[_nika__subcmd__arm__subcmd__migrate_commands] )) ||
_nika__subcmd__arm__subcmd__migrate_commands() {
    local commands; commands=()
    _describe -t commands 'nika arm migrate commands' commands "$@"
}
(( $+functions[_nika__subcmd__catalog_commands] )) ||
_nika__subcmd__catalog_commands() {
    local commands; commands=()
    _describe -t commands 'nika catalog commands' commands "$@"
}
(( $+functions[_nika__subcmd__check_commands] )) ||
_nika__subcmd__check_commands() {
    local commands; commands=()
    _describe -t commands 'nika check commands' commands "$@"
}
(( $+functions[_nika__subcmd__completions_commands] )) ||
_nika__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'nika completions commands' commands "$@"
}
(( $+functions[_nika__subcmd__dap_commands] )) ||
_nika__subcmd__dap_commands() {
    local commands; commands=()
    _describe -t commands 'nika dap commands' commands "$@"
}
(( $+functions[_nika__subcmd__doctor_commands] )) ||
_nika__subcmd__doctor_commands() {
    local commands; commands=()
    _describe -t commands 'nika doctor commands' commands "$@"
}
(( $+functions[_nika__subcmd__explain_commands] )) ||
_nika__subcmd__explain_commands() {
    local commands; commands=()
    _describe -t commands 'nika explain commands' commands "$@"
}
(( $+functions[_nika__subcmd__guard_commands] )) ||
_nika__subcmd__guard_commands() {
    local commands; commands=()
    _describe -t commands 'nika guard commands' commands "$@"
}
(( $+functions[_nika__subcmd__help_commands] )) ||
_nika__subcmd__help_commands() {
    local commands; commands=(
'list:List the workflows below this directory, one relative path per line' \
'welcome:The mirror\: what Nika is · what this machine already has (editors · local models · key presence · this workspace) · the next commands. Offline · presence-only · always exit 0 — a greeting, not a gate' \
'check:Audit a workflow BEFORE it runs\: plan · cost ceiling · secret flows · types · tools — every finding teaches its fix' \
'run:Run a workflow (the same audit runs first · live render)' \
'test:Golden test\: run under the MOCK provider (offline · deterministic) and compare the typed \`outputs\:\` against \`<file>.golden.json\`' \
'inspect:Static anatomy\: tasks · verbs · wave groups · cost · permits — and the ONE graph projector (\`--format json|mermaid|dot\` for the machine surfaces · human stays the default)' \
'explain:Teach one error code (cause · category · fix-form), a hint kind \`nika check\` printed in \`\[brackets\]\`, or narrate a workflow FILE\: what it does · the waves · cost before a token is spent · what it touches · how to run it' \
'key:The run-signing key lifecycle (mint · TOFU fingerprint · rotate — old pubs stay verifiable)' \
'arm:What this project has ARMED, and when each beat next fires. Read-only — it schedules nothing (the file proposes, the machine disposes). Exit \`0\` clean · \`2\` the registry refuses' \
'serve:Resident ARM firer by default (the SAME \`fire\`, wall clock in place of the OS). \`--bind\` + \`--workflows\` + \`--token-file\` opens authenticated loopback HTTP. Exit \`0\` clean · \`1\` otherwise' \
'sign:Sign a workflow file (S3 · author-binding)\: mint \`<file>.minisig\` · \`--check\` verifies' \
'doctor:Diagnose this machine (binary · config · provider keys · local models). Diagnose-only — prints the exact fix command, never mutates anything' \
'init:Found a repo (\`.vscode\` schema wiring · \`AGENTS.md\` · Cursor rule + MCP · \`.agents/skills\` authoring skill · optional workflow set). Bare on a terminal the founding wizard runs; flags are the scriptable twin. Existing files are skipped — \`--force\` overwrites' \
'wire:Wire Nika into editor/agent MCP clients (explicit, idempotent). The door\: \`detected --dry-run\` previews what this machine shows · \`detected\` wires it · \`<client>\` wires one · \`all\` is the advanced sweep (previewed, then confirmed or \`--yes\`)' \
'model:Local models — pull from the Hugging Face Hub, serve on this machine, list/rm the disk (ONE models dir · no external daemon)' \
'spec:The embedded spec identity (\`--canon\` prints the SSOT)' \
'catalog:The embedded provider/model catalog (models · capabilities · env vars)' \
'try:See a canonical workflow WORK — offline by default (the mock rehearsal · zero keys · zero flags), nothing written, nothing owned. Bare \`nika try\` lists what there is to see' \
'new:The ONE creation door\: describe the job in plain words (routes), or name a slug/skeleton (takes it, ingredients included) — the destination derives from the slug. Plain words route across jobs, lessons and skeletons. Bare \`nika new\` on a terminal is the guided flow; \`nika new '\''?'\''\` lists the skeleton set' \
'completions:Generate shell completions (bash · zsh · fish · elvish · powershell)' \
'trace:Read the flight recorder (replay or summarize a run)' \
'guard:The hook'\''s judge (hidden — the wired \`guard-run.sh\` shim calls it, agents never type it)\: read a host hook payload (\`--stdin\`) or one command line (\`--command\`), find every effective \`nika run\`, audit the EXACT file in-process, and answer the hook protocol. P0-7 + P0-15\: a red file or a priced model without \`--max-cost-usd\` is denied; an unjudgeable run is a VISIBLE \`guard_unavailable\`, never a silent allow. The run belongs to the human — guard JUDGES, it never executes' \
'dap:Debug Adapter Protocol server (stdio) — time-travel a recorded run under a debugger UI\: breakpoints on task lines · step forward AND back through settles · outputs in the variables pane. Replay re-renders, never re-executes' \
'lsp:Run the language server over stdio (drives the editor extension)' \
'mcp:Run the read-only MCP oracle (validate + learn — never execute). Default transport\: stdio; \`--transport http\` serves Streamable HTTP for managed hosts. \`approve\` re-pins tools from \`.nika/mcp_servers.json\`' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika help commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__arm_commands] )) ||
_nika__subcmd__help__subcmd__arm_commands() {
    local commands; commands=(
'fire:Fire ONE beat now, if it is due — the one firer (D2)\: on-time window · miss policy · overlap lock · per-tick ceiling · the firing record. Prints exactly one stdout line, always (D8)' \
'migrate:Explicitly upcast every W2 sidecar into the hash-chained ledger, verify each chain, and rebuild its projections. Idempotent and never silent\: every beat receives a verdict' \
'disarm:Teach the disarm gesture (law N4 — removing the line does NOT disarm; \`actif\: false\` + \`raison\:\` + \`jusqu_au\:\` does)' \
    )
    _describe -t commands 'nika help arm commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__arm__subcmd__disarm_commands] )) ||
_nika__subcmd__help__subcmd__arm__subcmd__disarm_commands() {
    local commands; commands=()
    _describe -t commands 'nika help arm disarm commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__arm__subcmd__fire_commands] )) ||
_nika__subcmd__help__subcmd__arm__subcmd__fire_commands() {
    local commands; commands=()
    _describe -t commands 'nika help arm fire commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__arm__subcmd__migrate_commands] )) ||
_nika__subcmd__help__subcmd__arm__subcmd__migrate_commands() {
    local commands; commands=()
    _describe -t commands 'nika help arm migrate commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__catalog_commands] )) ||
_nika__subcmd__help__subcmd__catalog_commands() {
    local commands; commands=()
    _describe -t commands 'nika help catalog commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__check_commands] )) ||
_nika__subcmd__help__subcmd__check_commands() {
    local commands; commands=()
    _describe -t commands 'nika help check commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__completions_commands] )) ||
_nika__subcmd__help__subcmd__completions_commands() {
    local commands; commands=()
    _describe -t commands 'nika help completions commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__dap_commands] )) ||
_nika__subcmd__help__subcmd__dap_commands() {
    local commands; commands=()
    _describe -t commands 'nika help dap commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__doctor_commands] )) ||
_nika__subcmd__help__subcmd__doctor_commands() {
    local commands; commands=()
    _describe -t commands 'nika help doctor commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__explain_commands] )) ||
_nika__subcmd__help__subcmd__explain_commands() {
    local commands; commands=()
    _describe -t commands 'nika help explain commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__guard_commands] )) ||
_nika__subcmd__help__subcmd__guard_commands() {
    local commands; commands=()
    _describe -t commands 'nika help guard commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__help_commands] )) ||
_nika__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'nika help help commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__init_commands] )) ||
_nika__subcmd__help__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'nika help init commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__inspect_commands] )) ||
_nika__subcmd__help__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'nika help inspect commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__key_commands] )) ||
_nika__subcmd__help__subcmd__key_commands() {
    local commands; commands=(
'init:Mint the run-signing key (idempotent — refuses to clobber without \`--force\`)' \
'trust:Print the public key + TOFU fingerprint to enroll on other machines' \
'rotate:Retire the current public key to the ledger and mint a fresh one (old journals stay verifiable against the retired pubs)' \
    )
    _describe -t commands 'nika help key commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__key__subcmd__init_commands] )) ||
_nika__subcmd__help__subcmd__key__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'nika help key init commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__key__subcmd__rotate_commands] )) ||
_nika__subcmd__help__subcmd__key__subcmd__rotate_commands() {
    local commands; commands=()
    _describe -t commands 'nika help key rotate commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__key__subcmd__trust_commands] )) ||
_nika__subcmd__help__subcmd__key__subcmd__trust_commands() {
    local commands; commands=()
    _describe -t commands 'nika help key trust commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__list_commands] )) ||
_nika__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'nika help list commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__lsp_commands] )) ||
_nika__subcmd__help__subcmd__lsp_commands() {
    local commands; commands=()
    _describe -t commands 'nika help lsp commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__mcp_commands] )) ||
_nika__subcmd__help__subcmd__mcp_commands() {
    local commands; commands=(
'approve:Re-pin the server'\''s CURRENT tool definitions after human review (the remediation a drift refusal names), printing the new pin set' \
    )
    _describe -t commands 'nika help mcp commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__mcp__subcmd__approve_commands] )) ||
_nika__subcmd__help__subcmd__mcp__subcmd__approve_commands() {
    local commands; commands=()
    _describe -t commands 'nika help mcp approve commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__model_commands] )) ||
_nika__subcmd__help__subcmd__model_commands() {
    local commands; commands=(
'serve:Serve a GGUF model — an OpenAI-compatible foreground server on 127.0.0.1 (Ctrl-C stops it · the banner says how workflows reach it)' \
'pull:Download a GGUF from the Hugging Face Hub into the ONE models dir (\`~/.nika/models\` — the same dir \`serve --model <id>\` resolves, by construction). Size prints BEFORE downloading; 2 GiB and over confirms (\`--yes\` for CI). An interrupted pull resumes from its \`.part\`. \`HF_TOKEN\` authenticates gated repos. This fetch is CLI-level, like \`registry\:\` pulls — a workflow'\''s \`permits\:\` never govern it' \
'list:What'\''s on disk\: id · size · file per GGUF — the ONE models dir printed once at top' \
'rm:Remove a pulled model\: \`owner/repo\` removes every quant (and the tokenizer beside them) · \`owner/repo\:QUANT\` one file. A no-match refuses, listing what IS there' \
    )
    _describe -t commands 'nika help model commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__model__subcmd__list_commands] )) ||
_nika__subcmd__help__subcmd__model__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'nika help model list commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__model__subcmd__pull_commands] )) ||
_nika__subcmd__help__subcmd__model__subcmd__pull_commands() {
    local commands; commands=()
    _describe -t commands 'nika help model pull commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__model__subcmd__rm_commands] )) ||
_nika__subcmd__help__subcmd__model__subcmd__rm_commands() {
    local commands; commands=()
    _describe -t commands 'nika help model rm commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__model__subcmd__serve_commands] )) ||
_nika__subcmd__help__subcmd__model__subcmd__serve_commands() {
    local commands; commands=()
    _describe -t commands 'nika help model serve commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__new_commands] )) ||
_nika__subcmd__help__subcmd__new_commands() {
    local commands; commands=()
    _describe -t commands 'nika help new commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__run_commands] )) ||
_nika__subcmd__help__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'nika help run commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__serve_commands] )) ||
_nika__subcmd__help__subcmd__serve_commands() {
    local commands; commands=()
    _describe -t commands 'nika help serve commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__sign_commands] )) ||
_nika__subcmd__help__subcmd__sign_commands() {
    local commands; commands=()
    _describe -t commands 'nika help sign commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__spec_commands] )) ||
_nika__subcmd__help__subcmd__spec_commands() {
    local commands; commands=()
    _describe -t commands 'nika help spec commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__test_commands] )) ||
_nika__subcmd__help__subcmd__test_commands() {
    local commands; commands=()
    _describe -t commands 'nika help test commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace_commands] )) ||
_nika__subcmd__help__subcmd__trace_commands() {
    local commands; commands=(
'replay:Re-render a run live (replay = re-render, NEVER re-execute)' \
'evidence:Export the evidence pack for one run (journal + manifest + receipt + VERIFY.md) — RAMS-15\: one door on a run'\''s dossier (read · export · prove), all under \`trace\`' \
'receipt:Read a run receipt — \`explain\` renders its readable projection (stable text · a READING, never a proof)' \
'show:Print the final card only' \
'ls:List the workspace trace store (\`.nika/traces/\`)\: age · size · workflow · terminal state (completed/failed/paused) · the resume-candidate marker (★ — the newest of each workflow, the trace retention never collects · ADR-100)' \
'rm:Remove traces from the store — one by name/path, \`--older-than <dur>\`, or \`--all\`. Removing a paused trace refuses without \`--force\` and names the unanswered prompt it would destroy (ADR-100)' \
'outputs:Browse per-task outputs\: verb · duration · tokens · bounded preview (full value\: \`trace peek\`)' \
'export:Project the journal to OTLP/JSON lines — every \`OTel\` tool becomes a viewer (drag into Jaeger UI ≥1.60 · POST lines to any OTLP/HTTP endpoint). Local file, zero collector, zero vendor' \
'verify:Verify the journal'\''s tamper-evidence chain (0.96+), then climb the proof ladder\: SEALED (the \`run_sealed\` signature verifies against a custody key) · ANCHORED (the \`<trace>.anchor.json\` sidecar verifies fully offline) · REPLAYED (--replay compares a fresh run). The HIGHEST honestly-attained tier is reported. Three refusals name themselves rather than hide in a tier\: TAMPERED · SEAL BURIED (lines chained AFTER the seal — appending needs only write access, so this is forgery, never a crash) · ANCHOR FORGED (a sidecar that vouches for nothing) · and SEAL UNATTRIBUTABLE (the seal names a key you do not hold\: the signature is NOT judged, which is a missing input and never evidence of forgery). Exit 0 the tier holds · 2 broken chain, forged seal or forged anchor · 3 unchained (pre-chain journal), a missing input, or a seal this host cannot attribute' \
'anchor:Notarize the journal head OUTSIDE the journal (S3)\: submit the post-seal head — signed with the run key — to the public Sigstore Rekor v2 transparency log plus an RFC 3161 timestamp, writing a detached \`<trace>.anchor.json\` sidecar. An explicit NETWORK act\: this verb IS the opt-in. Exit 0 anchored · 2 the journal refuses (broken/torn) · 3 no key/network' \
'reproduce:Is this run reproducible? Compare a recorded journal against a fresh one and classify every task\: reproduced · nondeterministic (same def+inputs, different output) · authored · environment · status-changed · unverifiable. Exit 0 reproduced · 2 diverged' \
'peek:Read ONE task'\''s full output + its identity (hashes · duration · tokens). \`--raw\` prints the exact value only (pipe it to jq)' \
'session:The session digest\: waves · the wave holder others waited for · the spend by verb · the wall. The four numbers \`nika-tui-core\` derives — every one of them gated by the predicate that says whether it may be claimed (a holder nobody waited for is not named; no holder is painted over a failure)' \
'flow:The data waterfall\: which output fed which task, with recorded sizes (plan bindings from the workflow file × sizes from the trace)' \
    )
    _describe -t commands 'nika help trace commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__anchor_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__anchor_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace anchor commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__evidence_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__evidence_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace evidence commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__export_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__export_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace export commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__flow_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__flow_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace flow commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__ls_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__ls_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace ls commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__outputs_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__outputs_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace outputs commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__peek_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__peek_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace peek commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__receipt_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__receipt_commands() {
    local commands; commands=(
'explain:Render a receipt'\''s readable projection (stable text · a READING, never a proof — \`verify\` owns the proof)' \
    )
    _describe -t commands 'nika help trace receipt commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__receipt__subcmd__explain_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__receipt__subcmd__explain_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace receipt explain commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__replay_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__replay_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace replay commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__reproduce_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__reproduce_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace reproduce commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__rm_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__rm_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace rm commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__session_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__session_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace session commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__show_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__show_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace show commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__trace__subcmd__verify_commands] )) ||
_nika__subcmd__help__subcmd__trace__subcmd__verify_commands() {
    local commands; commands=()
    _describe -t commands 'nika help trace verify commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__try_commands] )) ||
_nika__subcmd__help__subcmd__try_commands() {
    local commands; commands=()
    _describe -t commands 'nika help try commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__welcome_commands] )) ||
_nika__subcmd__help__subcmd__welcome_commands() {
    local commands; commands=()
    _describe -t commands 'nika help welcome commands' commands "$@"
}
(( $+functions[_nika__subcmd__help__subcmd__wire_commands] )) ||
_nika__subcmd__help__subcmd__wire_commands() {
    local commands; commands=()
    _describe -t commands 'nika help wire commands' commands "$@"
}
(( $+functions[_nika__subcmd__init_commands] )) ||
_nika__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'nika init commands' commands "$@"
}
(( $+functions[_nika__subcmd__inspect_commands] )) ||
_nika__subcmd__inspect_commands() {
    local commands; commands=()
    _describe -t commands 'nika inspect commands' commands "$@"
}
(( $+functions[_nika__subcmd__key_commands] )) ||
_nika__subcmd__key_commands() {
    local commands; commands=(
'init:Mint the run-signing key (idempotent — refuses to clobber without \`--force\`)' \
'trust:Print the public key + TOFU fingerprint to enroll on other machines' \
'rotate:Retire the current public key to the ledger and mint a fresh one (old journals stay verifiable against the retired pubs)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika key commands' commands "$@"
}
(( $+functions[_nika__subcmd__key__subcmd__help_commands] )) ||
_nika__subcmd__key__subcmd__help_commands() {
    local commands; commands=(
'init:Mint the run-signing key (idempotent — refuses to clobber without \`--force\`)' \
'trust:Print the public key + TOFU fingerprint to enroll on other machines' \
'rotate:Retire the current public key to the ledger and mint a fresh one (old journals stay verifiable against the retired pubs)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika key help commands' commands "$@"
}
(( $+functions[_nika__subcmd__key__subcmd__help__subcmd__help_commands] )) ||
_nika__subcmd__key__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'nika key help help commands' commands "$@"
}
(( $+functions[_nika__subcmd__key__subcmd__help__subcmd__init_commands] )) ||
_nika__subcmd__key__subcmd__help__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'nika key help init commands' commands "$@"
}
(( $+functions[_nika__subcmd__key__subcmd__help__subcmd__rotate_commands] )) ||
_nika__subcmd__key__subcmd__help__subcmd__rotate_commands() {
    local commands; commands=()
    _describe -t commands 'nika key help rotate commands' commands "$@"
}
(( $+functions[_nika__subcmd__key__subcmd__help__subcmd__trust_commands] )) ||
_nika__subcmd__key__subcmd__help__subcmd__trust_commands() {
    local commands; commands=()
    _describe -t commands 'nika key help trust commands' commands "$@"
}
(( $+functions[_nika__subcmd__key__subcmd__init_commands] )) ||
_nika__subcmd__key__subcmd__init_commands() {
    local commands; commands=()
    _describe -t commands 'nika key init commands' commands "$@"
}
(( $+functions[_nika__subcmd__key__subcmd__rotate_commands] )) ||
_nika__subcmd__key__subcmd__rotate_commands() {
    local commands; commands=()
    _describe -t commands 'nika key rotate commands' commands "$@"
}
(( $+functions[_nika__subcmd__key__subcmd__trust_commands] )) ||
_nika__subcmd__key__subcmd__trust_commands() {
    local commands; commands=()
    _describe -t commands 'nika key trust commands' commands "$@"
}
(( $+functions[_nika__subcmd__list_commands] )) ||
_nika__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'nika list commands' commands "$@"
}
(( $+functions[_nika__subcmd__lsp_commands] )) ||
_nika__subcmd__lsp_commands() {
    local commands; commands=()
    _describe -t commands 'nika lsp commands' commands "$@"
}
(( $+functions[_nika__subcmd__mcp_commands] )) ||
_nika__subcmd__mcp_commands() {
    local commands; commands=(
'approve:Re-pin the server'\''s CURRENT tool definitions after human review (the remediation a drift refusal names), printing the new pin set' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika mcp commands' commands "$@"
}
(( $+functions[_nika__subcmd__mcp__subcmd__approve_commands] )) ||
_nika__subcmd__mcp__subcmd__approve_commands() {
    local commands; commands=()
    _describe -t commands 'nika mcp approve commands' commands "$@"
}
(( $+functions[_nika__subcmd__mcp__subcmd__help_commands] )) ||
_nika__subcmd__mcp__subcmd__help_commands() {
    local commands; commands=(
'approve:Re-pin the server'\''s CURRENT tool definitions after human review (the remediation a drift refusal names), printing the new pin set' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika mcp help commands' commands "$@"
}
(( $+functions[_nika__subcmd__mcp__subcmd__help__subcmd__approve_commands] )) ||
_nika__subcmd__mcp__subcmd__help__subcmd__approve_commands() {
    local commands; commands=()
    _describe -t commands 'nika mcp help approve commands' commands "$@"
}
(( $+functions[_nika__subcmd__mcp__subcmd__help__subcmd__help_commands] )) ||
_nika__subcmd__mcp__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'nika mcp help help commands' commands "$@"
}
(( $+functions[_nika__subcmd__model_commands] )) ||
_nika__subcmd__model_commands() {
    local commands; commands=(
'serve:Serve a GGUF model — an OpenAI-compatible foreground server on 127.0.0.1 (Ctrl-C stops it · the banner says how workflows reach it)' \
'pull:Download a GGUF from the Hugging Face Hub into the ONE models dir (\`~/.nika/models\` — the same dir \`serve --model <id>\` resolves, by construction). Size prints BEFORE downloading; 2 GiB and over confirms (\`--yes\` for CI). An interrupted pull resumes from its \`.part\`. \`HF_TOKEN\` authenticates gated repos. This fetch is CLI-level, like \`registry\:\` pulls — a workflow'\''s \`permits\:\` never govern it' \
'list:What'\''s on disk\: id · size · file per GGUF — the ONE models dir printed once at top' \
'rm:Remove a pulled model\: \`owner/repo\` removes every quant (and the tokenizer beside them) · \`owner/repo\:QUANT\` one file. A no-match refuses, listing what IS there' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika model commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__help_commands] )) ||
_nika__subcmd__model__subcmd__help_commands() {
    local commands; commands=(
'serve:Serve a GGUF model — an OpenAI-compatible foreground server on 127.0.0.1 (Ctrl-C stops it · the banner says how workflows reach it)' \
'pull:Download a GGUF from the Hugging Face Hub into the ONE models dir (\`~/.nika/models\` — the same dir \`serve --model <id>\` resolves, by construction). Size prints BEFORE downloading; 2 GiB and over confirms (\`--yes\` for CI). An interrupted pull resumes from its \`.part\`. \`HF_TOKEN\` authenticates gated repos. This fetch is CLI-level, like \`registry\:\` pulls — a workflow'\''s \`permits\:\` never govern it' \
'list:What'\''s on disk\: id · size · file per GGUF — the ONE models dir printed once at top' \
'rm:Remove a pulled model\: \`owner/repo\` removes every quant (and the tokenizer beside them) · \`owner/repo\:QUANT\` one file. A no-match refuses, listing what IS there' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika model help commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__help__subcmd__help_commands] )) ||
_nika__subcmd__model__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'nika model help help commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__help__subcmd__list_commands] )) ||
_nika__subcmd__model__subcmd__help__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'nika model help list commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__help__subcmd__pull_commands] )) ||
_nika__subcmd__model__subcmd__help__subcmd__pull_commands() {
    local commands; commands=()
    _describe -t commands 'nika model help pull commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__help__subcmd__rm_commands] )) ||
_nika__subcmd__model__subcmd__help__subcmd__rm_commands() {
    local commands; commands=()
    _describe -t commands 'nika model help rm commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__help__subcmd__serve_commands] )) ||
_nika__subcmd__model__subcmd__help__subcmd__serve_commands() {
    local commands; commands=()
    _describe -t commands 'nika model help serve commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__list_commands] )) ||
_nika__subcmd__model__subcmd__list_commands() {
    local commands; commands=()
    _describe -t commands 'nika model list commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__pull_commands] )) ||
_nika__subcmd__model__subcmd__pull_commands() {
    local commands; commands=()
    _describe -t commands 'nika model pull commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__rm_commands] )) ||
_nika__subcmd__model__subcmd__rm_commands() {
    local commands; commands=()
    _describe -t commands 'nika model rm commands' commands "$@"
}
(( $+functions[_nika__subcmd__model__subcmd__serve_commands] )) ||
_nika__subcmd__model__subcmd__serve_commands() {
    local commands; commands=()
    _describe -t commands 'nika model serve commands' commands "$@"
}
(( $+functions[_nika__subcmd__new_commands] )) ||
_nika__subcmd__new_commands() {
    local commands; commands=()
    _describe -t commands 'nika new commands' commands "$@"
}
(( $+functions[_nika__subcmd__run_commands] )) ||
_nika__subcmd__run_commands() {
    local commands; commands=()
    _describe -t commands 'nika run commands' commands "$@"
}
(( $+functions[_nika__subcmd__serve_commands] )) ||
_nika__subcmd__serve_commands() {
    local commands; commands=()
    _describe -t commands 'nika serve commands' commands "$@"
}
(( $+functions[_nika__subcmd__sign_commands] )) ||
_nika__subcmd__sign_commands() {
    local commands; commands=()
    _describe -t commands 'nika sign commands' commands "$@"
}
(( $+functions[_nika__subcmd__spec_commands] )) ||
_nika__subcmd__spec_commands() {
    local commands; commands=()
    _describe -t commands 'nika spec commands' commands "$@"
}
(( $+functions[_nika__subcmd__test_commands] )) ||
_nika__subcmd__test_commands() {
    local commands; commands=()
    _describe -t commands 'nika test commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace_commands] )) ||
_nika__subcmd__trace_commands() {
    local commands; commands=(
'replay:Re-render a run live (replay = re-render, NEVER re-execute)' \
'evidence:Export the evidence pack for one run (journal + manifest + receipt + VERIFY.md) — RAMS-15\: one door on a run'\''s dossier (read · export · prove), all under \`trace\`' \
'receipt:Read a run receipt — \`explain\` renders its readable projection (stable text · a READING, never a proof)' \
'show:Print the final card only' \
'ls:List the workspace trace store (\`.nika/traces/\`)\: age · size · workflow · terminal state (completed/failed/paused) · the resume-candidate marker (★ — the newest of each workflow, the trace retention never collects · ADR-100)' \
'rm:Remove traces from the store — one by name/path, \`--older-than <dur>\`, or \`--all\`. Removing a paused trace refuses without \`--force\` and names the unanswered prompt it would destroy (ADR-100)' \
'outputs:Browse per-task outputs\: verb · duration · tokens · bounded preview (full value\: \`trace peek\`)' \
'export:Project the journal to OTLP/JSON lines — every \`OTel\` tool becomes a viewer (drag into Jaeger UI ≥1.60 · POST lines to any OTLP/HTTP endpoint). Local file, zero collector, zero vendor' \
'verify:Verify the journal'\''s tamper-evidence chain (0.96+), then climb the proof ladder\: SEALED (the \`run_sealed\` signature verifies against a custody key) · ANCHORED (the \`<trace>.anchor.json\` sidecar verifies fully offline) · REPLAYED (--replay compares a fresh run). The HIGHEST honestly-attained tier is reported. Three refusals name themselves rather than hide in a tier\: TAMPERED · SEAL BURIED (lines chained AFTER the seal — appending needs only write access, so this is forgery, never a crash) · ANCHOR FORGED (a sidecar that vouches for nothing) · and SEAL UNATTRIBUTABLE (the seal names a key you do not hold\: the signature is NOT judged, which is a missing input and never evidence of forgery). Exit 0 the tier holds · 2 broken chain, forged seal or forged anchor · 3 unchained (pre-chain journal), a missing input, or a seal this host cannot attribute' \
'anchor:Notarize the journal head OUTSIDE the journal (S3)\: submit the post-seal head — signed with the run key — to the public Sigstore Rekor v2 transparency log plus an RFC 3161 timestamp, writing a detached \`<trace>.anchor.json\` sidecar. An explicit NETWORK act\: this verb IS the opt-in. Exit 0 anchored · 2 the journal refuses (broken/torn) · 3 no key/network' \
'reproduce:Is this run reproducible? Compare a recorded journal against a fresh one and classify every task\: reproduced · nondeterministic (same def+inputs, different output) · authored · environment · status-changed · unverifiable. Exit 0 reproduced · 2 diverged' \
'peek:Read ONE task'\''s full output + its identity (hashes · duration · tokens). \`--raw\` prints the exact value only (pipe it to jq)' \
'session:The session digest\: waves · the wave holder others waited for · the spend by verb · the wall. The four numbers \`nika-tui-core\` derives — every one of them gated by the predicate that says whether it may be claimed (a holder nobody waited for is not named; no holder is painted over a failure)' \
'flow:The data waterfall\: which output fed which task, with recorded sizes (plan bindings from the workflow file × sizes from the trace)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika trace commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__anchor_commands] )) ||
_nika__subcmd__trace__subcmd__anchor_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace anchor commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__evidence_commands] )) ||
_nika__subcmd__trace__subcmd__evidence_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace evidence commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__export_commands] )) ||
_nika__subcmd__trace__subcmd__export_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace export commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__flow_commands] )) ||
_nika__subcmd__trace__subcmd__flow_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace flow commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help_commands] )) ||
_nika__subcmd__trace__subcmd__help_commands() {
    local commands; commands=(
'replay:Re-render a run live (replay = re-render, NEVER re-execute)' \
'evidence:Export the evidence pack for one run (journal + manifest + receipt + VERIFY.md) — RAMS-15\: one door on a run'\''s dossier (read · export · prove), all under \`trace\`' \
'receipt:Read a run receipt — \`explain\` renders its readable projection (stable text · a READING, never a proof)' \
'show:Print the final card only' \
'ls:List the workspace trace store (\`.nika/traces/\`)\: age · size · workflow · terminal state (completed/failed/paused) · the resume-candidate marker (★ — the newest of each workflow, the trace retention never collects · ADR-100)' \
'rm:Remove traces from the store — one by name/path, \`--older-than <dur>\`, or \`--all\`. Removing a paused trace refuses without \`--force\` and names the unanswered prompt it would destroy (ADR-100)' \
'outputs:Browse per-task outputs\: verb · duration · tokens · bounded preview (full value\: \`trace peek\`)' \
'export:Project the journal to OTLP/JSON lines — every \`OTel\` tool becomes a viewer (drag into Jaeger UI ≥1.60 · POST lines to any OTLP/HTTP endpoint). Local file, zero collector, zero vendor' \
'verify:Verify the journal'\''s tamper-evidence chain (0.96+), then climb the proof ladder\: SEALED (the \`run_sealed\` signature verifies against a custody key) · ANCHORED (the \`<trace>.anchor.json\` sidecar verifies fully offline) · REPLAYED (--replay compares a fresh run). The HIGHEST honestly-attained tier is reported. Three refusals name themselves rather than hide in a tier\: TAMPERED · SEAL BURIED (lines chained AFTER the seal — appending needs only write access, so this is forgery, never a crash) · ANCHOR FORGED (a sidecar that vouches for nothing) · and SEAL UNATTRIBUTABLE (the seal names a key you do not hold\: the signature is NOT judged, which is a missing input and never evidence of forgery). Exit 0 the tier holds · 2 broken chain, forged seal or forged anchor · 3 unchained (pre-chain journal), a missing input, or a seal this host cannot attribute' \
'anchor:Notarize the journal head OUTSIDE the journal (S3)\: submit the post-seal head — signed with the run key — to the public Sigstore Rekor v2 transparency log plus an RFC 3161 timestamp, writing a detached \`<trace>.anchor.json\` sidecar. An explicit NETWORK act\: this verb IS the opt-in. Exit 0 anchored · 2 the journal refuses (broken/torn) · 3 no key/network' \
'reproduce:Is this run reproducible? Compare a recorded journal against a fresh one and classify every task\: reproduced · nondeterministic (same def+inputs, different output) · authored · environment · status-changed · unverifiable. Exit 0 reproduced · 2 diverged' \
'peek:Read ONE task'\''s full output + its identity (hashes · duration · tokens). \`--raw\` prints the exact value only (pipe it to jq)' \
'session:The session digest\: waves · the wave holder others waited for · the spend by verb · the wall. The four numbers \`nika-tui-core\` derives — every one of them gated by the predicate that says whether it may be claimed (a holder nobody waited for is not named; no holder is painted over a failure)' \
'flow:The data waterfall\: which output fed which task, with recorded sizes (plan bindings from the workflow file × sizes from the trace)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika trace help commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__anchor_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__anchor_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help anchor commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__evidence_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__evidence_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help evidence commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__export_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__export_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help export commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__flow_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__flow_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help flow commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__help_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help help commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__ls_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__ls_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help ls commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__outputs_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__outputs_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help outputs commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__peek_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__peek_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help peek commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__receipt_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__receipt_commands() {
    local commands; commands=(
'explain:Render a receipt'\''s readable projection (stable text · a READING, never a proof — \`verify\` owns the proof)' \
    )
    _describe -t commands 'nika trace help receipt commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__receipt__subcmd__explain_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__receipt__subcmd__explain_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help receipt explain commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__replay_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__replay_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help replay commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__reproduce_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__reproduce_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help reproduce commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__rm_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__rm_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help rm commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__session_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__session_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help session commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__show_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__show_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help show commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__help__subcmd__verify_commands] )) ||
_nika__subcmd__trace__subcmd__help__subcmd__verify_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace help verify commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__ls_commands] )) ||
_nika__subcmd__trace__subcmd__ls_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace ls commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__outputs_commands] )) ||
_nika__subcmd__trace__subcmd__outputs_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace outputs commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__peek_commands] )) ||
_nika__subcmd__trace__subcmd__peek_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace peek commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__receipt_commands] )) ||
_nika__subcmd__trace__subcmd__receipt_commands() {
    local commands; commands=(
'explain:Render a receipt'\''s readable projection (stable text · a READING, never a proof — \`verify\` owns the proof)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika trace receipt commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__receipt__subcmd__explain_commands] )) ||
_nika__subcmd__trace__subcmd__receipt__subcmd__explain_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace receipt explain commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__receipt__subcmd__help_commands] )) ||
_nika__subcmd__trace__subcmd__receipt__subcmd__help_commands() {
    local commands; commands=(
'explain:Render a receipt'\''s readable projection (stable text · a READING, never a proof — \`verify\` owns the proof)' \
'help:Print this message or the help of the given subcommand(s)' \
    )
    _describe -t commands 'nika trace receipt help commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__receipt__subcmd__help__subcmd__explain_commands] )) ||
_nika__subcmd__trace__subcmd__receipt__subcmd__help__subcmd__explain_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace receipt help explain commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__receipt__subcmd__help__subcmd__help_commands] )) ||
_nika__subcmd__trace__subcmd__receipt__subcmd__help__subcmd__help_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace receipt help help commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__replay_commands] )) ||
_nika__subcmd__trace__subcmd__replay_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace replay commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__reproduce_commands] )) ||
_nika__subcmd__trace__subcmd__reproduce_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace reproduce commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__rm_commands] )) ||
_nika__subcmd__trace__subcmd__rm_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace rm commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__session_commands] )) ||
_nika__subcmd__trace__subcmd__session_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace session commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__show_commands] )) ||
_nika__subcmd__trace__subcmd__show_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace show commands' commands "$@"
}
(( $+functions[_nika__subcmd__trace__subcmd__verify_commands] )) ||
_nika__subcmd__trace__subcmd__verify_commands() {
    local commands; commands=()
    _describe -t commands 'nika trace verify commands' commands "$@"
}
(( $+functions[_nika__subcmd__try_commands] )) ||
_nika__subcmd__try_commands() {
    local commands; commands=()
    _describe -t commands 'nika try commands' commands "$@"
}
(( $+functions[_nika__subcmd__welcome_commands] )) ||
_nika__subcmd__welcome_commands() {
    local commands; commands=()
    _describe -t commands 'nika welcome commands' commands "$@"
}
(( $+functions[_nika__subcmd__wire_commands] )) ||
_nika__subcmd__wire_commands() {
    local commands; commands=()
    _describe -t commands 'nika wire commands' commands "$@"
}

if [ "$funcstack[1]" = "_nika" ]; then
    _nika "$@"
else
    compdef _nika nika
fi
