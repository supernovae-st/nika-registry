# The nika-spec commit the first-party showcases are projected from.
# Bump deliberately: edit this, run scripts/project_pack.py --write, review
# the new (immutable) entries. The projector reads THIS, never a floating
# HEAD — so CI and a local run project byte-identical entries.
d5f7b8cd86bd6e20aa4e8ba645c8139d2b1157eb
